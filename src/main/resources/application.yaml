spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/s7
    password: root
    username: postgres
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: validate
        dialect:

logging:
  level:
    root: INFO
  file:
    name: logback.log
    path: /