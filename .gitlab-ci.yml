image:
  name: docker/compose:1.22.0
  entrypoint: ["/bin/sh", "-c"]
stages:
  - build
  - deploy

gradle-build:
  image: gradle:latest
  stage: build
  script:
    - echo "Hello"
  rules:
    - if: $CI_COMMIT_BRANCH == "master"

deploy:
  stage: deploy
  script:
    - "gradle clean"
    - "gradle copyJarToRoot"
    - docker-compose down
    - docker-compose up -d
  rules:
    - if: $CI_COMMIT_BRANCH == "master"